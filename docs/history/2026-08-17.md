# 2026-08-17 工作日志

## 阶段：Agent 体系搭建（architect + docs + scribe）— 15:20

- **改动**：
  - 新增 `.opencode/agent/` 8 个 agent：`orchestrator`（主控，primary）、`architect`、`developer`、`qa`、`code-reviewer`、`docs`、`devops`、`scribe`（subagent）。
  - 新增 `.opencode/command/` 5 个命令：`test`、`review`、`doc`、`record`、`deploy`。
  - 新增根 `opencode.json`：`default_agent: orchestrator`。
  - 新增 `docs/adr/README.md`（ADR 约定与模板）与 `docs/history/2026-08-17.md`（本日志）。
  - 更新 `AGENTS.md`：Agent 角色说明、记录约定（history/ADR）。
- **原因**：参考 edge-reader 的 agent 职责分类与项目开发约定，为 tgdl-bot 建立大厂 8 环节流程（需求澄清 → 设计评审 → 分层开发 → 测试验收 → 代码评审 → 文档 → 部署 → 巡检）的主控调度体系；角色精简（无前端/入库细分，合并为 developer）。dev server 生命周期管理不适用（tgdl-bot 为长轮询 bot，无 dev server）。
- **结果**：dotnet 验证命令与 git 约定均按 tgdl-bot 的 AGENTS.md 适配（`dotnet build -c Release` 0 警告、`dotnet test`、dev 仅本地集成分支、feat/fix squash 合并、push/tag 由用户触发 CI）。
- **遗留**：无。重启 opencode 后生效。

## 阶段：开源合规 + i18n + 安装向导 + bot 配置管理 设计（architect）— 17:20

- **改动**：
  - 修复异常 git 状态：删除基于 dev 的异常分支，基于 `main`（dd8e557，含 agent 体系提交）重建 `feat/oss-i18n` 分支。
  - 新增设计文档 `docs/design-internationalization.md`（428 行），覆盖：开源合规（GPL v3、免责声明、SECURITY.md、合规说明）、i18n 中英双语可扩展、install.sh 交互式安装向导、bot 内配置管理、README 双语。
  - 新增 3 篇 ADR：`docs/adr/0001-i18n-架构.md`、`docs/adr/0002-bot-配置管理.md`、`docs/adr/0003-install-交互式安装向导.md`。
  - 提交：`f9fbda6`（设计方案）、`6fbdd5a`（3 篇 ADR），均在 `feat/oss-i18n` 分支。
- **原因**：用户需求——① 开源合规：GPL v3、免责声明、SECURITY.md、合规说明；② i18n 中英双语；③ install.sh 交互式安装；④ bot 内改配置（除 token/api id/api hash 外，安装时白名单/目标只读，bot 维护独立列表合并生效，改完自动重启并通知）；⑤ bot 语言默认跟随 Telegram 客户端，首次私聊弹选择，可用 `/language` 修改。设计决策依据见 ADR 0001–0003。
- **结果**：设计经 orchestrator 向用户简述，**用户已确认方案**，进入开发阶段。
- **遗留**：
  1. `/config set` 连接类键（如 bot token）改错可致重启失联——设计为可改+警告+删 overlay 恢复，需在开发中验证体验。
  2. zh-TW/zh-Hant 暂映射 zh，后续可扩展。
  3. 首次私聊语言选择弹窗存在并发竞态，需 `TryAdd` 去重。
  4. pending-notify 需在进程退出前同步写完，避免丢失通知。
   5. README.en.md 长期维护依赖 docs 流程，需在文档阶段落实。

## 阶段：开发批次 1——i18n 基础 D1-D5（developer）— 17:05

- **改动**：
  - 新增 `src/TGBot/Texts/I18n/` 模块：`II18n`、`I18nService`、`LanguageCatalog`（en/zh 目录维护）、`UserLanguageStore`、`UserLanguageResolver`、`BotLanguage`。
  - `UserTexts` 全部文本键常量化；en/zh 嵌入式资源各 60 键。
  - `InboundMessage` 增加 `Language` 传播；全调用点（MessageRouter、CommandHandler、DownloadCoordinator、Update 错误提示等）迁移到 i18n 渲染。
  - 新增首次私聊语言选择弹窗（`TryAdd` 去重，规避并发竞态）与 `/language` 命令。
- **原因**：用户需求「i18n 中英双语」落地，设计依据 ADR 0001（i18n 架构）；语言默认跟随 Telegram 客户端 `language_code`，首次私聊弹选择，`/language` 可改。
- **结果**：232 测试全绿、`dotnet build -c Release` 0 警告；提交 `b873104`（D1-D4 基础设施）→ `b34aedd`（D5 测试适配，232 项）。
- **遗留**：语言弹窗与 `/language` 的重复弹窗场景待 code review 与后续 P2 治理；zh-TW/zh-Hant 暂映射 zh。

## 阶段：开发批次 2——配置管理 D6-D9（developer）— 17:40

- **改动**：
  - `ConfigParser` 启动错误改双语（i18n）输出；新增配置键 `TgdlLanguage`、`StateDir`（含别名与校验）。
  - 新增 `src/TGBot/Config/Overlay/`：`OverlayStore`（持久化 overlay）、`OverlayApplier`（合并生效）、`AccessListMerge`（安装白名单/目标列表与 bot 维护列表合并）、`PendingNotifyStore`、`PendingNotifySender`。
  - 新增 `/config`（bot 内查看/修改配置，连接类键可改但带警告）与 `/access` 命令；改配置后自动重启 + 待通知机制（进程退出前同步写完 pending-notify，避免丢失）。
- **原因**：用户需求「bot 内改配置」（除 token/api id/api hash 外，安装时白名单/目标只读，bot 维护独立列表合并生效，改完自动重启并通知）；设计依据 ADR 0002（bot 配置管理）。
- **结果**：284 测试全绿、0 警告；提交 `618f11b`（D6 双语错误+新键）→ `d1f1070`（D9 测试适配，284 项）。
- **遗留**：`/config set` 连接类键改错可致重启失联——已设计为可改+警告+删 overlay 恢复，待 code review 验证。

## 阶段：qa 首次验收（qa）— 17:50

- **改动**：无源码改动；执行全量测试与验收。
- **原因**：8 环节流程「测试验收」节点，对 D1-D9 交付物做首次验收。
- **结果**：**通过**（290 测试，含真实集成测试）。遗留 1 项 P1：entrypoint 未写 `StateDir`（发布门禁，容器重建丢状态风险）。
- **遗留**：P1——entrypoint 需同步 `StateDir` 键（转 code-reviewer 确认后修复）。

## 阶段：code-reviewer 首轮审查（code-reviewer）— 18:10

- **改动**：无源码改动；输出审查结论。
- **原因**：8 环节流程「代码评审」节点。
- **结果**：**打回**，缺陷清单：
  - P0-1：docker 配置链路断裂——`StateDir` 未同步 entrypoint（`docker/docker-entrypoint.sh`），pull 重建丢状态（与 qa 的 P1 同源，升级为 P0）。
  - P1-1：引号值归一化缺失（`LocalApiBaseUrl` 等引号值落盘/校验不一致）；P1-2：状态文件权限未 0600；P1-3：`UpdateException` 错误中文直发未 i18n；P1-4：重启无节流；P1-5：配置中凭据（token 等）未脱敏。
  - P2：若干（键常量遗漏、弹窗治理、锁键、大小写解析等）。
- **遗留**：全部 P0/P1/P2 转修复阶段。

## 阶段：修复与运维——缺陷修复 + 开源合规并行（developer + devops）— 18:50

- **改动**：
  - developer（P1-1~P1-5 + P2 全部）：`NormalizeValue` 单一来源（引号值统一归一化）、状态文件 0600 权限、`UpdateFailureReason` 错误分类走 i18n、60s 重启节流、`DisplayValue` 凭据脱敏、键常量补齐、弹窗治理、StateDir 锁键、解析大小写不敏感；P2-6 决定**不拆分 CommandHandler**（理由：共享基础设施，避免行为变化）。
  - devops（P0-1 + O1-O6）：`docker/docker-entrypoint.sh` 写入 `StateDir`/`TgdlLanguage`、`docker/.env.example` 补键；新增 GPL v3 `LICENSE`、`NOTICE`、`SECURITY.md`；73 文件加 SPDX 头；`install.sh` 交互式向导（语言选择/必填校验/超 5 次跳过/无 TTY 降级）；README 徽章与合规声明。
- **原因**：修复 code-reviewer 首轮 P0/P1/P2 清单；用户需求「开源合规：GPL v3、免责声明、SECURITY.md、合规说明」与「install.sh 交互式安装」落地（ADR 0003）。
- **结果**：315 测试全绿、0 警告；`feat/oss-i18n` squash 合并 main（`acbf783`，89 文件 +7028/-607，含全部缺陷修复）。
- **遗留**：NOTICE 中 telegram-bot-api 许可证声明有误（见下轮复评 P1）；docfx 增量缓存风险待发版处理。

## 阶段：qa 补测 + code-reviewer 复评（qa + code-reviewer）— 19:00

- **改动**：qa 补测 2 例（`95ebb2f`：`LocalApiBaseUrl` 引号值校验通过与落盘归一化端到端）。
- **原因**：对修复阶段成果复验（引号值归一化是 P1-1 核心，需端到端补测）。
- **结果**：qa 317 测试全绿、0 警告。code-reviewer 复评**通过，可进入发布**；新 P1 1 项：NOTICE 许可证声明错误（telegram-bot-api 实为 **Boost Software License 1.0**，非 GPL）；P2 若干（i18n 模板花括号兜底、userinfo 脱敏、弹窗去重、同值去重、entrypoint bool、Dockerfile 冗余 ENV、install.sh 容器兜底）。
- **遗留**：新 P1（NOTICE 修正）与 P2 转收尾阶段。

## 阶段：docs + devops 收尾（docs + devops）— 19:20

- **改动**：
  - docs：`build-docs.sh` 0 警告重新生成 API 文档（`611b65e`，覆盖 I18n/Config.Overlay 新模块）；修正设计文档 StateDir 锁键描述；ADR 索引表维护。
  - devops：NOTICE 修正（`9f165bb`，telegram-bot-api → Boost Software License 1.0，ffmpeg 明确 GPL）；P2 修复（`eba2ac2`：i18n 模板花括号兜底防静默无响应、`LocalApiBaseUrl` userinfo 脱敏、/language 重复弹窗去重、/config 同值去重并入 config.conf 生效值；`507850d`：entrypoint bool 大小写不敏感、Dockerfile 删除冗余 ENV、install.sh 容器兜底校验 running 与镜像匹配）；`85cf593`：README.en.md 英文版（对照中文全量翻译，含配置表/合规声明/已知限制）。
- **原因**：闭环 code-reviewer 复评遗留的 P1/P2；满足「README 双语」与文档一致性要求。
- **结果**：323 测试全绿、0 警告、`bash -n` 通过、`dotnet list package --vulnerable` 无易受攻击包；当前 main 最新 `85cf593`。
- **遗留**：
  1. ADR 0002 中 StateDir 可改性与实现（锁键）不一致——docs 已同步设计文档，ADR 待 architect 评估标注（新增或撤销标记）。
  2. docfx 增量缓存风险：发版前建议 `rm -rf src/TGBot/obj/docfx` 再重新生成。
  3. 真实镜像构建与 GHCR 推送依赖 CI（本地无 `docker/dist` 产物）；发版后需人工核对镜像 ENV。
   4. 未 push、未打 tag——等用户指示（push 触发 CI 发版）。

## 阶段：开源社区化收尾——P0+P1 优化（developer + devops + docs）— 20:38

- **改动**：
  1. **README 双语互链与专业化**（`1212ead`，dev）：README.md 补 `[English](README.en.md)` 链接（与英文版互链对称）；润色：修正过时表述（Config 双语提示、third_party 说明）、引言重写突出自托管/i18n、特性补安装向导/双语界面/config-access 命令、术语统一、删重复句；README.en.md 轻度同步。
  2. **版本号源自构建**（`feat/oss-version-tag` → squash `b52e9a5`，dev）：csproj 加 `<Version>2.4.0</Version>`（CI 经 `-p:Version` 覆盖，注释说明）；新增 `AppInfo.cs` 从 AssemblyInformationalVersion 规范化读取（截断 `+commit` 后缀、剥 `v` 前缀、兜底 `0.0.0`）；`/status` 首行显示 `tgdl-bot v2.4.0`（新增 i18n 键 `StatusBotVersion`，en/zh 对称，键集合仍一致）；AppHost smoke 模式打印版本；新测试 `AppInfoTests`（Normalize 理论 13 例 + 稳定性）、`StatusCommandTests`（/status 链路 2 例）。
  3. **社区文档与模板**（`docs/oss-community` → squash `e71f64b`，dev）：新增 `CONTRIBUTING.md`（开发环境/0 警告门槛/分支规范/squash/PR 指引，双语）、`CODE_OF_CONDUCT.md`（Contributor Covenant 2.1 双语）、`CHANGELOG.md`（Keep a Changelog，v2.4.0 Unreleased，基于历史记录）；新增 `.github/ISSUE_TEMPLATE/bug_report.yml` + `feature_request.yml`（GitHub issue forms，提醒不贴 token）与 `PULL_REQUEST_TEMPLATE.md`。
  4. **CI 与工程化**（`chore/oss-ci` → squash `7c7ba7b`，dev）：新增 `ci.yml`（push dev/feat/fix/chore + pull_request 触发，restore→build（grep warning 硬门槛，pipefail）→test，permissions 最小化 read）；新增 `dependabot.yml`（nuget + github-actions，weekly）；新增 `.editorconfig`（LF/4 空格，调研确认现有代码全 LF）与 `.gitattributes`（text eol=lf + binary 类型）；release.yml：修正误导性权限注释（workflow 显式声明覆盖仓库默认设置，仓库无需改 read-only 默认）；Publish 步骤补 `-p:Version=${VERSION}`（此前缺失，tag v2.4.1+ 会与 csproj 默认脱节）。
- **原因**：社区化收尾 P0+P1 优化——版本号此前硬编码在 /status 文本中且与发布 tag 脱节，改为构建源单一来源（CI 可覆盖）；CI 此前仅发布流程（v* tag 触发），缺 PR/分支校验，补 ci.yml 建立门禁；社区协作缺 CONTRIBUTING/行为准则/CHANGELOG/issue 模板；.editorconfig/.gitattributes 统一跨平台换行与编码约定；release.yml 权限注释误导（仓库默认 read-only 即可，workflow 显式声明 write 覆盖）。
- **结果**：三分支 squash 合并 dev：`b52e9a5`、`e71f64b`、`7c7ba7b`；分支已删；工作树干净。验证：`dotnet build -c Release` 0 警告；`dotnet test` 340/340 通过。未 push、未打 tag、未合并 main。
- **遗留**：
  1. Actions 仓库默认权限保持 read-only 即可（workflow 显式声明 write 覆盖）。
  2. 本地 csproj 默认版本 2.4.0 需随下次发布 tag 同步（CI 会覆盖，本地默认仅影响未覆盖构建）。
  3. 版本号下次递增时（v2.4.1+）release.yml 已由 `-p:Version` 保证一致。

## 阶段：arm64 多架构支持——设计（architect）— 20:56

- **改动**：新增 `docs/design-arm64-support.md`（设计文档）与 `docs/adr/0004-多架构镜像发布与架构感知更新.md`（ADR 0004）。决策：CI matrix 双 job（x64=ubuntu-latest、arm64=ubuntu-24.04-arm 官方免费原生 ARM runner）+ per-arch tag + manifest job（`buildx imagetools create` 合并出 `{ver}`/`latest` multi-arch tag + `inspect` 断言双平台 + 建 Release）；否决 QEMU 模拟方案（慢 3-5 倍、构建产物无法原生校验）；`/update` 用 `RuntimeInformation.ProcessArchitecture` 运行时检测架构，未知架构快速失败；telegram-bot-api 资产缺失时 CI 硬失败。
- **原因**：用户需求 arm64 支持。选原生 ARM runner 而非 QEMU 模拟：官方免费、构建产物可在真实 arm64 上校验；运行时架构检测保证容器镜像与实际二进制匹配，避免 x64 镜像在 arm64 上拉错资产。
- **结果**：设计经评审确认，提交 `6e56560`（dev 分支）。
- **遗留**：后续按命名约定修订（见下节）微调术语。

## 阶段：arm64 多架构支持——命名约定修订（architect）— 21:05

- **改动**：用户指出 amd64/arm64 太像易看错，自命名统一为 **x64/arm64**：dist 目录 `docker/dist/{x64,arm64}/`、per-arch tag `{ver}-x64`、矩阵变量 `distarch`。Docker 标准平台串（`linux/amd64`）与第三方官方资产命名（ffmpeg `amd64-static`、yt-dlp `aarch64` 等）**保留**并在文档加注对应关系。Dockerfile 新增 `ARG DISTARCH`（COPY 不能用 shell 替换，须显式 ARG 驱动路径），`TARGETARCH` 保留仅 deno 分支使用。
- **原因**：x64/arm64 视觉区分度高、不易混淆；Docker 平台串与第三方资产名不可自命名，须保留标准值并显式对应。
- **结果**：设计文档与 ADR 0004 同步修订，提交 `ae0c268`（dev 分支）。
- **遗留**：无。

## 阶段：arm64 多架构支持——/update 架构感知（developer）— 21:26

- **改动**：新增 `ToolArch` 静态类（`FfmpegReleaseUrl`/`YtDlpReleaseUrl` 按 `Architecture` 映射，X64/Arm64 返回完整 URL，其他抛 `InvalidOperationException` 且含架构名）；`FfmpegToolSource`/`YtDlpToolSource` 构造函数加可选 `Func<Architecture>? archProvider`（默认 `RuntimeInformation.ProcessArchitecture`），AppHost 调用点零改动；新增 `ToolArchTests` 125 行（注入 `() => Architecture.Arm64` 覆盖 arm64 分支——本机 x64 下唯一覆盖途径；断言完整 URL）。
- **原因**：/update 下载 ffmpeg/yt-dlp 资产须匹配运行架构（容器内由 `TARGETARCH` 决定）；注入式架构提供者使 arm64 分支可在 x64 主机单测覆盖。
- **结果**：`dotnet build -c Release` 0 警告、测试全绿；提交 `64b03ac`（dev 分支）。
- **遗留**：无。

## 阶段：arm64 多架构支持——CI matrix（devops）— 21:26

- **改动**：`release.yml` 重构：build job 引入 matrix（`distarch`/`platform`/`runs-on`/`dotnet-rid`/`tba-asset`/`ffmpeg-url`/`ytdlp-asset` 七变量，`fail-fast:false`）；12 步骤含产物四文件齐全断言、ELF/xz 魔数校验+重试、per-arch 构建（`build-args DISTARCH`）、x64 job 专属冒烟（`if: matrix.distarch == 'x64'`）；manifest job（`needs: build`，重新登录 GHCR，`imagetools create -t {ver} -t latest`，`inspect` grep 双平台断言 `exit 1`，Release 建于此 job）；Dockerfile 4 条 COPY 改 `dist/${DISTARCH}/`（yt-dlp 由跨架构共用改 per-arch）；AGENTS.md/README/README.en.md/CHANGELOG 同步。
- **原因**：落地 ADR 0004 的 matrix + manifest 双平台发布流水线；原生 ARM runner 构建产物经魔数校验保证真实性；冒烟测试仅 x64 跑（arm64 runner 无 docker 权限）。
- **结果**：提交 `a1bf399`（dev 分支）。
- **遗留**：见阶段结果（未 push 未验证 CI 实跑，待发版触发）。

## 阶段：arm64 多架构支持——qa 验收（qa）— 21:3x

- **改动**：无源码改动；全量测试与验收。
- **原因**：8 环节流程「测试验收」节点。
- **结果**：**通过**。352 测试全绿、0 警告、集成测试真实运行通过；`ToolArchTests` 注入覆盖确认（arm64 分支经架构注入验证）；实现与设计一致（仅 1 处 P2 文档偏差：设计文档第 201 行实现描述与代码不符）。
- **遗留**：P2 文档偏差转 code-reviewer 清单。

## 阶段：arm64 多架构支持——code-reviewer 审查（code-reviewer）— 21:3x

- **改动**：无源码改动；输出审查结论。
- **原因**：8 环节流程「代码评审」节点。
- **结果**：**通过**。0 P0/P1；8 条 P2（文件末尾换行、Dockerfile 交叉断言缺失、文档描述过时、grep 断言可收紧等）。
- **遗留**：8 条 P2 转修复阶段，其中 P2-1~P2-3 修复，P2-4~P2-8 为可选优化暂不修。

## 阶段：arm64 多架构支持——P2 修复（developer）— 21:39

- **改动**：`ToolArch.cs`/`ToolArchTests.cs` 补文件末尾换行（`.editorconfig insert_final_newline`）；Dockerfile 加 DISTARCH/TARGETARCH 交叉断言 RUN（`if/elif/else` 分支——reviewer 建议的 `&&/||` 写法因 POSIX sh 左结合会误判，已实测修正）；docker 实测三组合（arm64+arm64 过、amd64+x64 过、arm64+x64 失败）。
- **原因**：闭环 P2 清单；交叉断言防错误 dist 目录落入错误架构镜像（构建期而非运行期暴露）。
- **结果**：提交 `e8c9003`（dev 分支）。
- **遗留**：无。

## 阶段：arm64 多架构支持——文档（docs）— 21:45

- **改动**：docfx 重新生成 0 警告（新增 `TGBot.Update.ToolArch` 页面，并补齐此前缺失的 `AppInfo` 页面）；`docs/design-arm64-support.md` 第 201 行实现描述同步（常量→调用点动态解析）；ADR 0004 末尾追加勘误小节（不改已采纳正文，符合 ADR 约定）。
- **原因**：闭环 qa 的 P2 文档偏差；ADR 决策变更不修改旧正文、以勘误小节追加。
- **结果**：`./build-docs.sh` 0 警告；提交 `8f2a6b1`（dev 分支）。
- **遗留**：build-docs.sh 与 docfx metadata 的 restore 副作用（`project.assets.json` 被覆盖导致 publish `--no-restore` 报 NETSDK1047，需 devops 后续优化）；docfx 增量缓存需发版前清缓存核对 ToolArch 页面。

## 阶段：arm64 多架构支持——阶段汇总（orchestrator 判定）— 21:45

- **改动**：全部 commit 已 squash 合并 dev（设计 `6e56560`/`ae0c268`、实现 `64b03ac`/`a1bf399`/`e8c9003`/`8f2a6b1`），分支已删，当前仅剩 dev/main；工作树干净。
- **原因**：阶段闭环判定。
- **结果**：验证通过——`dotnet build -c Release` 0 警告；`dotnet test` 352 全绿（一次偶发网络集成测试失败，重跑通过，与历史瞬时 TLS 问题同类）。未 push、未打 tag、未合并 main（等用户指示触发 CI 发版）。
- **遗留**：
  1. arm64 镜像真实运行待用户 ARM 设备实测（CI 只到构建+原生资产校验+manifest 断言）。
  2. 每次发版 GHCR 遗留 2 个 per-arch tag（`{ver}-x64`/`{ver}-arm64`）——不可变资产，可接受。
  3. tba arm64 资产依赖 fork 持续发布（当前已存在；缺失时 CI 硬失败属预期）。
  4. build-docs.sh/docfx restore 副作用（NETSDK1047）待 devops 优化；发版前清 docfx 增量缓存核对 ToolArch 页面。
   5. P2-4~P2-8（grep 断言收紧、tba ELF 架构校验、测试文案断言等）为可选优化，未修。

## 阶段：跨平台文档构建工具——设计（architect）— 22:00

- **改动**：
  - 背景：用户指出 `build-docs.sh` 是 Linux bash 脚本，Windows/macOS 无法原生使用；要求文档脚本也用 C# 写实现跨平台。用户选择**方案 B**（独立 C# 工具项目 `tools/TgdlDocBuilder`，`dotnet run --project tools/TgdlDocBuilder` 一条命令三平台一致）；**不做** install.sh（部署脚本保持 Linux）、**不做** /update 资产平台化。
  - 新增 `docs/design-doc-builder.md`（设计文档）与 `docs/adr/0005-跨平台文档构建工具.md`（ADR 0005）。四步编排：docfx 检测 → 清增量缓存 → publish 生成 XML（不带 `--no-restore`，解决 NETSDK1047）→ docfx build（`--warningsAsErrors`）。
  - 方案对比：A. MSBuild target（docfx 是全局工具无法 PackageReference，编排别扭）/ B. 独立工具（**选中**）/ C. bash+ps1 双脚本（双倍维护）。
- **原因**：文档构建工具须三平台一致（Windows/macOS/Linux），纯 C# 实现避免双脚本双倍维护与行为分叉；NETSDK1047 根因是 docfx metadata 的 restore 副作用覆盖 `project.assets.json`，publish 不带 `--no-restore` 让其自行恢复以规避；`--warningsAsErrors` 保持 0 警告硬门槛。
- **结果**：设计经评审确认，提交 `fba1c3e`（dev 分支）。
- **遗留**：无。

## 阶段：跨平台文档构建工具——实现（developer）— 22:30

- **改动**（`feat/doc-builder` `190fcaf` + `d08d79a` → squash 合并 dev `c7f71ae`，分支已删）：
  - 新增 `tools/TgdlDocBuilder/`：`Program.cs`（348 行）+ csproj，net10.0，**零 PackageReference**；工具自身不加 `GenerateDocumentationFile`，但编译仍 0 警告（tools 不产文档、不进发布镜像，AGENTS.md 已同步该界定）。
  - docfx 回退链：`docfx` → `dotnet docfx` → 打印安装指引（标准安装场景正常；本机 ELF 直装故回退链在本机不可用）。
  - 缓存清理递归清 ReadOnly 属性（Windows 只读坑）；进程调用全 `ArgumentList` 无 shell（杜绝注入）；输出不重定向、实时透传（保留 docfx 进度与颜色）。
  - `DOTNET_ROOT_<ARCH>` 遮蔽坑修复：仅当用户**显式**设置 `DOTNET_ROOT` 时才移除子进程的 `DOTNET_ROOT_<ARCH>` 变量（不违背此前"不干预"决策——用户显式设置始终生效）。
  - 删除 `build-docs.sh`；README×2 / CONTRIBUTING / AGENTS.md / `.opencode/agent/docs.md` / `.opencode/command/doc.md` / `docfx/index.md` 命令同步为 `dotnet run --project tools/TgdlDocBuilder`。
- **原因**：落地 ADR 0005 与设计文档；消除 build-docs.sh 的 Linux 平台绑定，三平台一条命令一致。
- **结果**：`dotnet build -c Release` 0 警告；`dotnet test` 352 全绿；提交 `c7f71ae`（dev 分支，HEAD）。
- **遗留**：无。

## 阶段：跨平台文档构建工具——qa 验证（qa）— 22:40

- **改动**：无源码改动；对工具做验收。
- **原因**：8 环节流程「测试验收」节点，验证新工具替代 build-docs.sh 的完整性与跨平台准备度。
- **结果**：**通过**——`--help` 输出正常；未知参数 `exit 1`；完整跑 0 警告 0 错误（docfx 2.78.5）；增量重跑 0 警告；临时 publish 目录无残留；主构建 0 警告；352 测试全绿。
- **遗留**：见阶段汇总。

## 阶段：跨平台文档构建工具——阶段汇总（orchestrator 判定）— 22:45

- **改动**：设计 `fba1c3e` + 实现 squash `c7f71ae` 已合并 dev（当前 dev HEAD = `c7f71ae`，分支已删），工作树干净。
- **原因**：阶段闭环判定。
- **结果**：验证通过——工具完整跑与增量重跑均 0 警告、主构建 0 警告、352 测试全绿。未 push、未打 tag、未合并 main（等用户指示）。
- **遗留**：
  1. 三平台验收待 Windows/macOS 实机（Windows 只读删除、`dotnet docfx` 回退链、PATH 场景）。
  2. 本机 docfx 为 ELF 直装（非 dotnet tool shim），`dotnet docfx` 回退链在本机不可用（标准安装场景正常）。
  3. docfx 2.78.5 增量缓存为内容级比对（旧版 `obj/docfx` 清理逻辑保留为兼容 no-op）。

## 阶段：/update ffmpeg 换源 BtBN——背景（orchestrator 判定）— 08-18 12:34

- **改动**：CI 侧先换源——`05dd661`（release.yml 的 ffmpeg 静态构建源改 `BtbN/FFmpeg-Builds`，johnvansickle.com 在 CI 持续不可达）；用户态 `/update` 仍走 johnvansickle.com（不稳定已知问题）。
- **原因**：用户指示统一换源并自动抓取最新日期构建，消除 johnvansickle.com 的不稳定依赖。
- **结果**：进入设计阶段。
- **遗留**：无。

> 注：本阶段相关提交时间戳为 08-18 12:34–13:42（git 本地时区），系 08-17 晚间工作的次日延续，按指示记入本日志。

## 阶段：/update ffmpeg 换源 BtBN——设计（architect）— 08-18 12:55

- **改动**：
  - 新增设计文档 `docs/design-update-ffmpeg-btbn.md`（177 行）与 ADR 0006（`docs/adr/0006-将ffmpeg源切换为BtbN.md`），提交 `39df786`。
  - 要点：BtBN `latest` 是滚动 tag（无语义版本），用 GitHub API `releases/tags/latest` 的 `published_at`（ISO 8601 UTC）归一化为 `2026.08.17.13.29.26` 作版本标识；固定 latest URL 放 `ToolArch`。
  - **核心难点**：本地二进制报 git 计数（N-118503）与远端日期标度不同不能直接比较 → 新增 marker 文件 `<installPath>.autobuild` 记录上次安装时间 + `ToolVersion.IsDateLike` 标度一致性判断，保留「已是最新不下载」语义。
  - xz 魔数校验（`fd 37 7a 58 5a 00`）防 200 坏响应；API 限流 60/h 评估充足；yt-dlp 的 HEAD 302 解析模式因 BtBN 直连 CDN 无法复用。
- **原因**：用户指示统一换源、自动抓取最新日期构建；决策依据 ADR 0006。
- **结果**：设计经评审确认，提交 `39df786`。
- **遗留**：实现阶段承接。

## 阶段：/update ffmpeg 换源 BtBN——实现（developer）— 08-18 13:20

- **改动**（`c2ad0fc`，10 文件 +538/-30）：
  - `ToolArch` 的 ffmpeg 静态构建 URL 换 BtBN（与 CI 逐字符一致）。
  - `FfmpegToolSource` 改用 GitHub API + 请求级 UA + 非 200 → null。
  - `UriVersionParser` 删 johnvansickle HTML 解析、加 `ParseGitHubApiPublishedAt`（正则含可选 `Z`——实测 API 响应带 `Z`，修正设计初版）。
  - 新增 `ToolVersion.IsDateLike`；新增 `FfmpegVersionMarker`（原子写 tmp + `File.Move`）；`Updater` 短路逻辑 + 写 marker。
  - 26 个新测试（`FfmpegVersionMarkerTests`、`ToolArchTests`/`UpdaterTests`/`UpdaterIntegrationTests` 扩展）。
- **原因**：落地设计文档与 ADR 0006。
- **结果**：`dotnet build -c Release` 0 警告、378 测试全绿，提交 `c2ad0fc`。
- **遗留**：转 qa/code-reviewer。

## 阶段：/update ffmpeg 换源 BtBN——验收与审查（qa + code-reviewer）— 08-18 13:30

- **改动**：无源码改动；qa 验收 + code-reviewer 审查。
- **原因**：8 环节流程「测试验收」「代码评审」节点。
- **结果**：
  - qa **通过**：实现与设计逐条一致、集成测试真实执行、P2 观察 3 项。
  - code-reviewer **有条件通过**：P1-1——122MB 下载受共享 HttpClient 110s 超时约束（慢链路必失败）；P1-2——marker 残留 + 二进制缺失 → 永久「已是最新」失去自愈；P2 若干。
- **遗留**：P1 两项转修复。

## 阶段：/update ffmpeg 换源 BtBN——P1 修复（developer）— 08-18 13:42

- **改动**（`24b2e0d`，6 文件 +209/-37）：
  - 下载独立超时：`CreateLinkedTokenSource` + `CancelAfter` 10min，构造参数化可测；yt-dlp ~30MB 无同风险不动。
  - marker 短路加 `File.Exists` 校验（二进制缺失回退二进制解析抛 `LocalVersionUnavailable`，恢复自愈）。
  - 补 3 测试（含短超时取消、marker+二进制缺失两场景）；P2-3 文件尾换行、P2-7 测试用 `MarkerPath()`。
- **原因**：闭环 code-reviewer 有条件通过的 P1/P2 清单。
- **结果**：0 警告、381 测试全绿，提交 `24b2e0d`。
- **遗留**：转阶段汇总。

## 阶段：/update ffmpeg 换源 BtBN——阶段汇总（orchestrator 判定）— 08-18 13:45

- **改动**：main 累积提交 `05dd661` → `39df786` → `c2ad0fc` → `24b2e0d`；工作树干净，main HEAD = `24b2e0d`（其后无 docs 提交）。
- **原因**：阶段闭环判定。
- **结果**：orchestrator 亲自验证——`dotnet build -c Release` 0 警告、`dotnet test` 381 全绿。全部未 push、未打 tag（等用户指示）。
- **遗留**：
  1. P2 backlog：Updater 与 `FfmpegVersionMarker` 的 stringly-typed 特判（OCP 记账，后续可下沉 `ILocalVersionProvider`）；marker 写失败静默无日志；集成测试 HttpClient 无超时/403 不跳过；非 200 状态码日志不可区分；等值短路与损坏 marker 用例可补。
  2. 容器内手动验证：首次 `/update` 下载 122MB 生成 marker；再次 `/update` 显示「已是最新（2026.08.x...）」。
  3. GitHub 整体故障未恢复：CI 全绿验证待 Actions 恢复后 re-run；分支保护、PR 合并、push main 待用户指示。
